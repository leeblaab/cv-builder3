{
  "entities": {
    "users": {
      "fields": {
        "cvData": {
          "type": "map",
          "description": "The current original or edited CV data of the user"
        },
        "tailorResult": {
          "type": "map",
          "description": "The last successful AI tailored CV results and matching score"
        },
        "plan": {
          "type": "string",
          "description": "User subscription plan: free, pro, or unlimited"
        },
        "monthlyUsage": {
          "type": "number",
          "description": "How many CV downloads or tailorings the user has performed this month"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the last update"
        }
      },
      "indexes": []
    }
  },
  "firestore": {
    "rules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /users/{userId} {\n      allow read, write: if request.auth != null && request.auth.uid == userId;\n    } \n  }\n}"
  }
}
